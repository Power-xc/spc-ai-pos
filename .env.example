# docker-compose reads this file (copy it to `.env`) for the local Postgres
# password. The same value is used by the `postgres` service and by the
# backend's DATABASE_URL, so you only set it in one place.
#
#   cp .env.example .env
#
# Use any throwaway value for local development.
POSTGRES_PASSWORD=change-me-local-dev
